[project]
name = "modelith-emit-pydantic"
version = "0.1.1"
description = "Modelith Pydantic v2 data-model emitter"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
dependencies = ["modelith-core"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mdl_emit_pydantic"]

[tool.uv.sources]
modelith-core = { workspace = true }
